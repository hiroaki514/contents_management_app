ユーザ追加画面
<%= form_with(model: @user) do |f| %>
  <%= f.label 'ユーザ名' %>
  <%= f.text_field :name %>
    <%= f.label 'メールアドレス' %>
  <%= f.text_field :email %>

  <div>
    <%= f.submit '送信' %>
  </div>
<% end %>